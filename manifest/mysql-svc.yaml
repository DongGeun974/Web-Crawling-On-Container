apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
spec:
  selector:
    app: mysql-deploy
  ports:
  - protocol: TCP
    port: 3306  # Service의 포트
    targetPort: 3306  # 컨테이너가 노출하는 포트
  type: LoadBalancer  # External IP를 할당하여 외부로 노출
